# sm_120 인계 검증 결과 — 트랙 C

2026-08-17 · A 트랙 인계 사항(검토 "미해소 리스크") 이행 · 실측 환경: RTX 5060 Ti 16GB
(sm_120, cap 12.0), Windows 11, uv venv(wt/C), `torch 2.11.0+cu128`

배분 사항: QLoRA 4bit 1 step부터 확인하고, 실패 시 즉시 보고한다.

## 1. 결과 요약

| # | 대상 | 버전 | 결과 | 비고 |
|---|---|---|---|---|
| 1 | **bitsandbytes QLoRA 4bit 1 step** | 0.50.1 | **PASS** | NF4 양자화(Linear4bit) + LoRA(r16) backward + AdamW step. loss 유한, peak 0.59GB (Qwen2.5-0.5B 스모크 — 커널 가용성 검증 목적. 4B 실 프로파일은 16GB 실측 계획 §5에서) |
| 2 | **ultralytics 학습** | 8.4.120 (스펙 핀과 일치) | **PASS** | yolo11n, coco8, 1 epoch, 320px — 학습·검증·AMP 전 구간 sm_120 동작. cu128 커널 정상 |
| 3 | **flwr[simulation] + ray** | flwr 1.33.0 / ray 2.55.1 | **PASS (import)** | Windows에서 설치·import 정상. 시뮬레이션 실구동(라운드 간 GPU 메모리 해제 포함)은 파일럿 항목 |
| 4 | **flash-attn** | — | **설치 불가 확정** | PyPI에 휠 자체가 없음(sdist 전용, `--no-build` 해석 실패로 실측). Windows+sm_120 사전 빌드 부재 → **SDPA 폴백 확정** (스펙 §5-2에 이미 반영) |
| 5 | **vllm** | 0.11.0 (해석만) | **로컬 사용 불가 판정** | uv 해석 결과 `torch 2.11.0+cu128 → 2.8.0(PyPI)` **강등을 요구** — sm_120 필수 조건(cu128)과 정면 충돌, Windows 네이티브 미지원. **평가 추론 vLLM은 Linux GPU(RunPod/WSL2) 확보 시점으로 이월, 로컬 폴백 = transformers generate** (greedy 1회라 결과 동일, 배치 처리량만 손해) |

**미검증으로 남는 것** (16GB 실측 계획·파일럿에서 확인): Qwen 4B 실 프로파일(peak VRAM·
step 시간), Ray 액터의 라운드 간 GPU 메모리 해제, vLLM의 Linux 환경 검증.

## 2. 스모크 중 실측으로 잡은 숨은 기본값 (사냥 지속 지시 이행 — 신규 2건)

| # | 숨은 기본값 | 증상 (실측 로그) | 처방 |
|---|---|---|---|
| 3 | **`optimizer='auto'`가 명시 lr0·momentum을 조용히 무시** | `'optimizer=auto' found, ignoring 'lr0=0.01' and 'momentum=0.937'... AdamW(lr=0.000119)` — 우리가 넘긴 SGD 하이퍼가 **경고 한 줄로 버려지고 AdamW로 교체됨** | 함정 #1 처방의 `optimizer='SGD'` 명시가 필수임을 실측 확인. **회계 매트릭스에 실제 사용 optimizer·lr 기록 컬럼 추가** — 설정과 실사용의 불일치를 회계로 잡는다 |
| 4 | **Ultralytics의 MLflow 자동 연동 + 전역 settings의 runs_dir 재지향** | mlflow 패키지가 설치돼 있자 자동으로 MLflow 초기화 시도. 출력이 CWD가 아니라 **전역 settings.json의 runs_dir(저장소 루트 `runs/`)** 로 감 | 트레이너 래퍼에서 Ultralytics settings의 `mlflow=False` 고정(로깅은 우리 서버 시점 단일 경로만 — 이중 로깅 방지), `project=` 절대경로 명시로 출력 위치 고정. 스모크 잔여물 `runs/`는 삭제 완료 |

(#1 `final_eval()` best.pt 로드, #2 `accept_failures=True`에 이은 3·4번째. #1은 이번
스모크에서도 "Validating ...best.pt..." 로그로 재확인됨.)

## 3. 재현 방법

스모크 스크립트 2본은 세션 스크래치패드에 있으며(`qlora_4bit_smoke.py`,
`yolo_flwr_smoke.py`), 구현 단계에서 `scripts/check_env.py`(A 소유)에 통합을 제안한다 —
QLoRA 1 step 검사를 env 체크에 넣으면 환경 재구축 때마다 4bit 커널 가용성이 자동 확인된다.
