# 숨은 기본값 레지스트리 — 트랙 C

2026-08-17 · 게이트 #8 후속("사냥 계속" 지시) · 유지 문서 — 발견 즉시 추가하고 게이트에 보고

**패턴 정의**: 전 건이 같은 형태다 — **"우리가 선언·설정한 것과 프레임워크의 실제 동작이
다르고, 그 불일치가 지표에 흔적을 남기지 않는다."** 공통 처방 원칙 2개를 전 건에 적용한다:
(a) **선언이 아니라 실사용을 기록한다** — 실효 설정(optimizer, lr, generation config,
절단 발생 수)을 런타임에 읽어 MLflow·회계 매트릭스에 로깅. (b) **불일치를 실패로 승격한다**
— assert로 선언≠실사용이면 run을 중단시킨다.

| # | 프레임워크 · 기본값 | 깨뜨리는 선언 | 증거 | 처방 | 상태 |
|---|---|---|---|---|---|
| 1 | Ultralytics `final_eval()`이 **best.pt를 로드·검증** | last 채점 (모델정책 2) | 소스 확인 + 스모크 로그 "Validating ...best.pt" 재확인 | `final_eval` no-op 오버라이드, 채점기는 best 파일명 즉시 실패(D) | 처방 확정 (12a 함정#1) |
| 2 | Flower FedAvg `accept_failures=True` — 클라이언트 실패 라운드를 나머지로 성립 | R×E=N 학습량 등가 | Flower 기본 파라미터 | `False` 강제 + 클라이언트×라운드 회계 매트릭스(머지 차단 조건) | **게이트 #6 결정 A 확정** |
| 3 | Ultralytics `optimizer='auto'` — **명시한 lr0·momentum을 경고 한 줄로 폐기하고 AdamW로 교체** | 5칸 공통 고정 '최적화'(SGD 0.937, cosine) | 스모크 실측 로그: `ignoring 'lr0=0.01' and 'momentum=0.937'... AdamW(lr=0.000119)` | `optimizer='SGD'` 명시 + **회계 매트릭스에 실사용 optimizer·lr 컬럼** | **게이트 #8 승인** |
| 4 | Ultralytics **MLflow 자동 연동**(mlflow 설치 감지 시) + **전역 settings.json runs_dir 재지향** | MLflow 로컬 단일 경로(서버 시점만 로깅) / 저장소 위생 | 스모크 실측: 저장소 루트 `runs/`에 출력 + MLflow 초기화 시도 | settings `mlflow=False` 고정, `project=` 절대경로 명시 | **게이트 #8 승인** |
| 5b | **채택 모델 실측 (2026-08-21)** — `Qwen/Qwen3.5-4B` 리포에는 `generation_config.json`이 **없다.** 즉 #5 경로는 채택 모델에서 발생하지 않는다 | — | HF 파일 목록 실측 (`23_모델확정_Qwen35_C.md`) | **처방은 그대로 유지한다.** 부재를 확인한 것과 명시 고정하는 것은 다른 일이고, 리포에 파일이 추가될 수 있다. 대신 실효 generation config 로깅이 "부재 확인"의 증거도 겸한다 | 확인 완료 |
| 5 | **Qwen 배포 `generation_config.json`이 샘플링을 기본으로 켠다** — `do_sample: true, temperature 0.7, top_p 0.8, top_k 20, repetition_penalty 1.1`. `model.generate()`는 이 파일을 기본 병합하므로 **override 없이 부르면 greedy 선언과 무관하게 샘플링으로 돈다** | **greedy(온도 0) 1회** (모델정책 4, 5칸 공통 고정 '디코딩') | **실측** — 로컬 캐시 Qwen2.5-0.5B-Instruct의 generation_config.json 원문 (동 계열 4B도 동일 관례) | 추론 진입점(`vlm/infer_eval.py`, `rag/judge`)에서 GenerationConfig를 **백지에서 명시 구성**(do_sample=False, temperature·top_p·top_k 미설정, repetition_penalty=1.0, max_new_tokens 고정), `assert do_sample==False`, **실효 generation config 전체를 MLflow 태그로 로깅.** vLLM 이월 시(Linux)에도 SamplingParams(temperature=0) 명시 | **신규 — 실측 확정, 게이트 보고** |
| 6 | **TRL `SFTConfig.max_length` 기본 1024 + 초과 시 조용한 절단**(truncation_mode keep_start) | max_seq_length **2,048** + **초과 샘플은 자르지 말고 폐기 후 건수 보고** (구현설계 6-2) | **소스 확인** — 설치본 trl 1.10.0 `sft_config.py` L69-74: "defaults to 1024", "Sequences longer than max_length are truncated" | 학습 전 전처리에서 **토큰 길이 필터로 명시 폐기 + 폐기 건수 MLflow 로깅**(불변조건 이행) → 필터 후 `max_length=2048` 설정 시 절단 발생 0을 **assert**(이중 방어). 기본값 1024 방치 시 판정문 데이터가 선언의 절반에서 잘린다 | **신규 — 소스 확정, 게이트 보고** |

| 7 | **`MetricRecord`가 `int \| float \| list[int] \| list[float]` 만 받고 `str`·`bool` 을 거부한다.** 같은 계층의 `ConfigRecord` 는 `str`·`bool`·`bytes` 를 전부 허용한다 | 라운드마다 올리는 **실사용 optimizer 이름·정본 키 다이제스트** 기록 (게이트 #8 승인, 회계 매트릭스 필수 컬럼) | **실측** — flwr 1.33.0, `metricrecord.py` L52 `is_valid` 가 `TypeError` 를 던진다. 허용 매트릭스 실측: MetricRecord `int/float/list` OK, `str/bool` X · ConfigRecord 전부 OK | 수치는 `MetricRecord`, 문자열(`keys-digest`·`optimizer`·`arg-optimizer`·`canonical-keys`)은 **`ConfigRecord` 로 분리**해 같은 `RecordDict` 에 싣는다. 분리 이유를 코드 주석에 남긴다 — 취향이 아니라 자료형 제약이라 나중에 합치려는 손이 온다 | **신규 — 실측 확정, 게이트 #9 등재 지시** |
| 8 | **Flower Message API `FedAvg` 의 노드 수 기본값이 전부 2다** — `min_train_nodes=2`, `min_evaluate_nodes=2`, `min_available_nodes=2`. `fraction_evaluate` 기본 1.0 | **3클라이언트 전수 참여** (C1·C2·C3 가 매 라운드 전부 돌아야 `R × E = N` 이 성립) | **실측** — flwr 1.33.0 `FedAvg.__init__` 시그니처 기본값 | `WeldFedAvg` 생성자가 셋을 **`expected_nodes` 로 강제 고정**하고 `fraction_evaluate=0.0`·`min_evaluate_nodes=0` 을 하드코딩한다. 기본 2를 두면 클라이언트 하나가 빠져도 라운드가 성립하고 가중치가 재정규화된다 | **신규 — 실측 확정, 수색 표면에서 승격** |

## 다음 수색 표면 (구현 착수 시 우선 확인)

- PEFT `save_embedding_layers="auto"` 승격 조건 2종 — 12a 함정 #3에서 기포착, 구현 시 재확인
- transformers `TrainingArguments.save_total_limit` 회전 삭제가 last 체크포인트를 지우는 경로
- transformers `Trainer` 재개 시 스케줄러·RNG 상태 복원 범위 (FL 라운드 재시작과 상호작용)
- ~~Flower `fraction_fit`/`min_evaluate_clients` 기본값~~ → **#8 로 승격 (확인 완료)**
- **Ray 액터의 CUDA 컨텍스트 잔존** (라운드 간 VRAM 해제) — 16GB 1장에서 검출 클라이언트
  3개를 순차로 돌릴 때 해제가 안 되면 **라운드 2에서 OOM**이다. 파일럿 계획은
  `32_Ray메모리_파일럿계획_C.md`
- vLLM(이월) 기본 SamplingParams·chat template 주입 여부
