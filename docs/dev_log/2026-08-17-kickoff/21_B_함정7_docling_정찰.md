# [B] 함정 #7 실측 보고 — 원문 확보 + Docling 정찰 (2026-08-17)

허용치 CSV 원문 수집과 Docling 파싱 착수 결과다.
스펙 §3-7이 W2 초입으로 지정한 "KR 규정 표 조판 실태 게이트 실측"을 앞당겨 완료했다.
**본 추출 파이프라인 구현이 아니라 정찰이다** — 산출물은 `corpus/parse/survey/`에 있고,
스펙의 본 경로 디렉터리(`docling/`·`accepted/`)와 분리했다.

## 1. 확보 문서

| doc_id | 문서 | 규모 | sha256 (앞 12) | 라이선스 |
|---|---|---|---|---|
| KR-RULES-P2 | 2025 선급 및 강선규칙/적용지침 2편 재료 및 용접 (합본, krs.co.kr 공개) | 362쪽 12.2MB | `46c4ee189f4a` | OPEN |
| IACS47 | IACS Rec.47 Rev.10 Corr.1 (Oct 2025, CLN, 공식 S3) | 67쪽 1.4MB | `ac54b7904b3f` | OPEN |

등록부 `corpus/parse/sources.yaml` 작성 완료 (KS-ST·KS-AL·KCI-JWJ는 pending 등록).
원본 PDF는 재배포 조건 미확인으로 git 미추적 — 해시·URL·접근일로 추적 (RAW.sha256).

## 2. 환경 실측 — sm_120 발견 1건 (C 트랙 공유 요망)

**triton 휠 부재로 torch.compile이 Docling layout 모델에서 치명 오류로 승격된다.**
경고 후 폴백이 아니라 변환 전체가 죽는다. `TORCHDYNAMO_DISABLE=1`(eager 강제)로 해소했고
survey 스크립트에 내장했다. → **sm_120/Windows에서 torch.compile 의존 경로는 전부 같은
지뢰다.** C의 QLoRA·vLLM 검증 시 참고 사항이다. (부수 교훈: `cmd | tail` 파이프가 파이썬 실패를
exit 0으로 가렸다 — 이후 검증 스크립트는 파이프 없이 로그 파일로 받는다.)

Docling 2.120.1 / TableFormer ACCURATE / OCR off. KR 21쪽 27.8s, IACS 67쪽 27.2s (GPU eager).

## 3. KR 실측 — 예상 최악 시나리오 회피 + 주 원천 특정

- **디지털 조판이다** (362쪽 중 저텍스트 2쪽). §3-7의 "괘선 그래픽 스캔본이면 경로 D 전멸"
  리스크는 **비발생**. Docling 경로 유효.
- **부록 2-7 "선체 용접이음부의 비파괴검사 기준" (p316~336) = RT 판정 주 원천.**
  표 24개 추출, 핵심 표 품질 양호 (빈 셀 비율 0 다수):
  - 표면결함 합격기준 표(t011): **균열·융합부족 "허용하지 않음"(불허 실물)**, 표면 기공
    "d≤0.25t, 최대 3mm, 필릿은 d≤0.25a", 언더컷 2단 조건부 한계
  - 종별구분·결함점수·판정기준 표(t012~t014): **KS B 0845 계열 점수제 재수록** — 결함
    긴지름→점수(1~25), 두께 구간별 최대결함크기(4 / 5 / **t/5** / 10) + 허용 점수 합
  - RT 내부결함 위임 표(t008): ISO 10675-1:2021 **합격 등급 1/2/3 매핑** (수치는 유료 표준)

## 4. 발견 4건과 처리 방침 (스키마 동결 유지 — 컬럼 변경 없음)

| # | 발견 | 처리 |
|---|---|---|
| 1 | **점수제 체계** (결함점수 합산 판정) — `limit_rule` enum{const, prop_t, prop_t_cap, none_permitted}에 없는 형태 | v1: 단일 결함 **최대결함크기 행만 active** 전사 (t/5는 prop_t factor=0.2로 표현 가능 — 기존 스키마로 충분). 점수 합산·인접 간격 단서는 `scope=excluded` + 건수 보고 (스펙의 기존 F6 배제 메커니즘 그대로). **점수제 정식 지원 여부는 총괄 판단 판단 요청** — 채택 시 limit_rule 확장은 동결 변경이라 승인 필요 |
| 2 | **ratio_basis=a 실물** (필릿 d≤0.25a) | 스펙 `ratio_basis` 컬럼 실증. 열린 질문 #3 기본값(s=t 가정, a 발견 시 보류 큐) 유지 — 단 a가 실재 확인됐으므로 보류 큐 처리 규칙이 실제로 발동될 예정 |
| 3 | **KR이 KS·ISO 수치를 재수록** | `restated_from` 경로 실증. KS 열람(경로 M) 의존이 줄어들 가능성 — KS-ST 열람 전에 KR 재수록분 커버리지를 먼저 산정한다. ISO 10675-1 위임 표는 등급 매핑만 있고 수치가 없으므로 열린 질문 #1 기본값(유료 전용 수치 미편입) 그대로 |
| 4 | **∠ 문자가 원문 조판** (Docling 오독 아님 — pdfplumber 원문 대조로 확인) | 변환표(∠→<) 등재 대상. "미등재 문자 중단" 규칙(§3-4)이 실전에서 필요함을 실증 |

## 5. IACS 실측 — 병리 1건

- 표 49개. **Standard/Limit 2단 체계 실물** 확인 → `quality_scheme=iacs` 필요성 실증.
- **병리: 특수 글리프(θ, ≤, °) 텍스트 레이어 소실.** "θ ≥ 60°, h ≤ 6 mm"가
  "60 h 6 mm"로 추출된다 (일부 셀은 ≤ 보존 — 폰트별 상이). 수치 파싱률·전 토큰 소진
  원칙이 걸러야 할 대표 사례이며, 해당 표는 수용 판정에서 거부 → 경로 P 재시도 또는
  수기 판정 큐 예상. empty>0.1 표 31/49 — Standard 열 공란(Limit만 규정) 구조가 다수.

## 6. 다음 단계

1. **본 추출 파이프라인 구현** (TableExpectation·수용 판정기·parse_limit_cell·변환기) —
   함정 #7 본역이므로 **다관점 병렬 검증으로 착수** (총괄 게이트 후 계획 수립 계획에 편입).
2. limits.csv 초판 전사 대상 우선순위: KR 부록 2-7 표면결함 표 + 최대결함크기 표 →
   IACS47 용접 관련 표 (글리프 소실 표는 P/M 경로) → KS 커버리지 공백분만 열람 전사.
3. KCI 학회지 (b) QA 원문 수집은 별도 트랙으로 계속.
