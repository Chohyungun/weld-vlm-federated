# 게이트 — 전처리 규격 확정 (8/25)

총괄 · 2026-08-25 · 40번 판정의 일정 재편(8/25 규격 확정 / 8/28 분할 동결)에 따른 첫 게이트

## 판정: 통과. 전처리 규격을 확정한다.

| 전제 | 실물 확인 |
|---|---|
| 매니페스트 v0 | `data/interim/manifest_v0` — 이미지 62,996 / 인스턴스 109,140 / 묶음 6,328 |
| 예비 분할 | train 45,353 / eval 12,600 / val 5,043 · C1 26,323 / C2 16,918 / C3 7,155 (잠그지 않음) |
| 캘리브레이션 | 예비 분할 학습 풀에서만 수행. 평가셋 행 미접근 |
| `preprocess.tile` | `configs/base.yaml` — 1280×720, k=1, `tau_mode: band_containment`, `selection: sha256(image_id+seed)`, `padding: forbidden` |
| 폐기 상한·회계 | 정상 셀 ≤5% + `oversized_band_cropped` 별도 셀 (밴드 초과 4.05% 는 중심 크롭) |
| 사전등록 상수 | `evaluation/prereg.py` — 전량양성 0.2081 / 규격전용 0.3025 / 순기여 +0.0944 |

## 확정으로 잠기는 것

- 타일 규격 1280×720, k=1, 결정론적 선택(시드 고정), 내용 기반 선택 금지
- τ = 밴드 포함(band ⊆ tile), 초과분은 밴드 중심 크롭
- 용입부족 2장 제외 (계약 동결 유지)
- **"데이터 확정" = 매니페스트 v0 + 이 규격.** 분할 동결은 8/28 프로브 판정 후다

## 다음

A 가 타일링 + RT 전량 재인코딩 → 매니페스트 v1 (8/26~27).
v1 에서 재확인할 것: Dirichlet 재추첨 횟수(v0 에서 15회 — pHash 엣지로 묶음이 굵어지면
밴드 폭 재검토), 인코딩 지문 단일화(P4).
