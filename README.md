# 데이터 반출이 제한된 환경에서의 비전-언어 모델 기반 용접 결함 진단
## 연합·개별·중앙집중 학습의 성능 비교

Vision-Language Models for Weld Defect Diagnosis in Data-Restricted Environments:
A Comparative Study of Federated, Local, and Centralized Learning

대한산업공학회 2026 추계학술대회 포스터 발표 예정. 초록 10/20, 논문 11/10, 발표 12/3~4.
제목은 가제이며 확정 시 갱신한다.

---

## 어떤 문제를 다루는가

조선소에서 용접이 끝나면 그 부위를 X선으로 찍어 안쪽을 확인한다. 겉으로는 멀쩡해 보여도
용접부 안에 기공이나 균열이 남아 있을 수 있기 때문이다. 검사원이 필름을 보고 결함을
찾아낸 다음, 그 크기가 규정에서 정한 허용치를 넘는지 따져 합격 여부를 정한다.

이 판단을 AI가 도울 수 있는지가 이 연구의 출발점이다.

문제는 학습 데이터다. AI에게 판단을 가르치려면 검사 이미지가 많이 필요한데, 이 이미지에는
어느 선박의 어느 부위를 어떻게 용접했는지가 그대로 담긴다. 기업 입장에서는 품질 정보이자
생산 정보라 밖으로 내보내기 어렵다. 여러 회사의 데이터를 한곳에 모아 학습시키는 흔한
방식은 시작조차 하기 어렵다.

## 연합학습으로 우회할 수 있는가

연합학습은 데이터를 모으지 않는다. 각 회사가 자기 데이터로 모델을 학습하고 데이터 대신
학습으로 바뀐 모델의 값만 중앙에 보낸다. 중앙은 받은 값들을 합쳐 공동 모델을 만들고 다시
각 회사에 내려준다. 이 과정을 여러 번 반복한다. 검사 이미지는 회사 밖으로 나가지 않는다.

이론상 성립하는 이야기다. 실제로 쓸 만한 성능이 나오는지는 별개다. 데이터를 모아 학습한
것과 비교해 얼마나 따라잡는지를 잰다.

## 무엇을 묻는가

**첫째, 데이터를 모으지 못하는 조건에서 공동 AI를 학습할 수 있는가.**
데이터를 모아 학습한 경우와 비교해 성능이 얼마나 떨어지는지, 그 손실을 현장에서
감당할 수 있는지 측정한다. 이 연구의 주된 기여다.

**둘째, 그 조건에서는 AI를 어떻게 구성하는 것이 나은가.**
아래 두 방식을 같은 조건에서 돌려 비교한다.

**셋째, 데이터가 적은 회사도 참여할 이유가 있는가.**
자체 데이터만으로 학습한 경우보다 연합에 참여했을 때 얼마나 나아지는지 따진다.
특수한 재질을 다루거나 검사 물량이 적은 회사에게 참여 유인이 있는지가 관건이다.

## 비교하는 두 가지 방식

**하나는 한 번에 처리한다.** 용접 사진을 이미지와 글을 함께 다루는 모델에 넣으면
결함 판단과 판정문이 나온다. 사람이 필름을 보고 소견을 쓰는 것과 비슷한 형태다.

**다른 하나는 둘로 나눈다.** 먼저 검출 모델이 사진에서 결함을 찾아 종류와 위치, 크기를
숫자로 뽑는다. 그 숫자를 받아 언어모델이 규정에서 해당 조항을 찾고 허용치와 대조해
합격 여부와 근거를 정리한다.

앞을 통합형, 뒤를 분리형이라 부른다. 통합형은 구조가 단순하고 분리형은 판단 근거를
조항 단위로 추적할 수 있다. 어느 쪽이 현장에 맞는지는 해 봐야 안다.

## 실험 구조

학습 방식 세 가지와 위 두 방식을 조합해 다섯 경우를 돌린다.

|            | 회사별 단독 학습 | 데이터를 모아 학습 | 연합학습 |
|------------|----------------|------------------|---------|
| **통합형** | 제외            | ○                | ○       |
| **분리형** | ○              | ○                | ○       |

통합형을 회사별로 단독 학습하는 경우는 뺐다. 회사마다 큰 모델을 혼자 구축하는 상황이
현실적인 도입 형태가 아니고 학습 비용도 가장 크다. 참여 이득은 분리형
비교로 답할 수 있다.

## 성공을 무엇으로 판단하는가

데이터를 모아 학습한 쪽이 상한이고 회사별 단독 학습이 하한이다. 연합학습이 그 사이
어디에 놓이는지를 하나의 수치로 나타낸다.

```
회복률 = (연합 − 단독) / (모아서 학습 − 단독) × 100%
```

100%면 데이터를 모은 것과 같고 0%면 혼자 학습한 것과 다르지 않다.

기준이 되는 성능 지표는 결과를 보기 전에 정해 두었다. 결과를 확인한 뒤 유리한 지표를
골라 쓰는 것을 막기 위해서다.

## 공정한 비교를 위해 고정한 것

다섯 경우를 같은 조건에서 돌렸다는 점에 이 연구의 주장이 걸려 있다. 한쪽만 유리하게
학습되면 비교가 성립하지 않는다. 그래서 아래를 규칙으로 두었다.

- 성능이 좋아 보이는 시점에 학습을 멈추지 않는다. 정해진 양을 채운 마지막 결과로 채점한다.
- 채점용 데이터는 회사별로 나누기 전에 먼저 떼어 둔다. 같은 용접부를 연달아 찍은 사진은
  묶어서 한쪽으로만 보낸다. 같은 부위가 학습과 채점에 나뉘어 들어가면 성능이 부풀려진다.
- 다섯 경우의 출력을 같은 형식으로 바꾼 뒤 하나의 채점기로 채점한다.
- 답을 만든 모델이 자기 답을 검증하지 않는다. 추론은 한 번만 하고 다시 시도하지 않는다.
- 학습 데이터와 분할 결과에 해시를 붙이고 이후 다시 만들지 않는다.

이런 규칙은 적어 두는 것과 실제로 지켜지는 것이 다르다. 사용하는 도구의 기본 설정이
위 규칙을 조용히 무너뜨리는 경우를 여덟 건 찾아 처방과 함께
[기록해 두었다](docs/dev_log/2026-08-17-kickoff/22_숨은기본값_레지스트리_C.md).
선언을 믿지 않고 실제 동작을 기록한다. 어긋나면 실패로 처리한다.

## 저장소 구조

```
configs/      라벨 대응표, 실험 설정
data/         수집 → 변환 → 중복 묶기 → 분할
corpus/       규정 파싱, 허용치 표, 학습용 문답 생성과 검증
detection/    결함 검출 모델 학습
vlm/          이미지와 글을 함께 다루는 모델 학습
fl/           연합학습 서버와 클라이언트
rag/          규정 조항 검색
evaluation/   공통 출력 형식, 단일 채점기, 지표
tracking/     실험 기록
docs/         설계 문서와 개발 기록
```

## 데이터

원본 이미지와 학습된 모델은 저장소에 넣지 않는다. 어떤 파일을 어떻게 나눴는지를 적은
목록과 그 해시만 둔다.

규정 문서도 원문을 다시 배포하지 않는다. 어디서 받은 어느 판본인지와 해시, 그리고 파싱
스크립트를 남겼다. 원문을 직접 받으면 같은 결과가 나온다. 저장소에는 조항 번호와
허용치 수치처럼 사실에 해당하는 정보만 둔다.

## 개발 기록

`docs/dev_log/`에 설계 명세와 교차 검토, 판정 기록을 남겼다. 어떤 결정을 왜 내렸고
무엇이 반려됐는지가 그대로 있다. 중간에 뒤집힌 판단도 지우지 않았다.

## 인용

사용하는 데이터셋은 각자의 인용 요건을 따른다. RIAWELC을 쓰면 Totino 외(2022)와
Perri 외(Manufacturing Letters), Intel Robotic Welding을 쓰면 Stemmer 외(2024,
arXiv:2409.02290)를 인용한다. 상세는 [`docs/논문_리스트.md`](docs/논문_리스트.md)에 있다.

## 라이선스

AGPL-3.0. 전문은 [`LICENSE`](LICENSE)에 있다.

## 팀

조현건, 김예서, 박상은
