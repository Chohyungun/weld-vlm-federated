# 데이터 반출이 제한된 환경에서의 비전-언어 모델 기반 용접 결함 진단
## 연합·개별·중앙집중 학습의 성능 비교

Vision-Language Models for Weld Defect Diagnosis in Data-Restricted Environments:
A Comparative Study of Federated, Local, and Centralized Learning

대한산업공학회 2026 추계학술대회 포스터 발표 예정. 초록 10/20, 논문 11/10, 발표 12/3~4.
제목은 가제이며 확정 시 갱신한다.

---

## 연구의 출발점

조선 현장의 용접 검사 데이터는 기업의 품질정보와 생산정보를 담고 있어 외부 반출이 어렵다.
연합학습에서는 데이터를 한곳에 모으지 않고 각 기업이 로컬에서 학습한 모델의 업데이트
정보만 주고받는다. 이 연구는 그 방식이 실제 검사 업무에 쓸 만한 성능을 내는지 검증한다.

같은 조건에서 통합형 비전-언어 모델과 검출·판정 분리형 구조를 함께 돌려
어느 방식이 현장에 맞는지도 비교한다.

## 연구 질문

RQ1. 데이터를 못 모으는데 공동 AI를 어떻게 학습할 것인가. 이 연구의 주 기여다.
중앙집중 학습 대신 연합학습을 적용했을 때 성능 손실이 현장에서 감내할 수준인지 측정한다.

RQ2. 연합학습 환경에서는 어떤 구조가 적합한가. 통합형 하나가 이미지 이해부터 판정까지
수행하는 방식과, 검출 모델이 결함을 찾고 언어모델이 규정을 검색해 판정하는 방식을
같은 조건에서 비교한다.

RQ3. 데이터가 적거나 분포가 다른 기업도 참여할 가치가 있는가. 특수 재질이나 소규모
데이터만 가진 기업이 자체 학습 대비 얼마나 이득을 보는지 따진다.

## 실험 구조

학습 방식 세 가지와 모델 구조 두 가지를 조합해 다섯 칸을 돌린다.

|            | 로컬 단독 | 중앙집중 | 연합학습 |
|------------|----------|---------|---------|
| **통합형** | 제외      | 통합·중앙 | 통합·연합 |
| **분리형** | 분리·로컬 | 분리·중앙 | 분리·연합 |

통합형은 용접 사진을 비전-언어 모델에 넣어 결함 판단과 판정문 작성을 한 번에 수행한다.
분리형은 검출 모델이 결함의 종류와 위치, 크기를 숫자로 뽑고 언어모델이 규정을 검색해
합격 여부와 근거 조항을 정리한다.

통합형·로컬은 뺐다. 기업마다 대형 모델을 단독으로 구축하는 상황이 현실적인 도입 형태가
아니기 때문이다. 참여 이득은 분리형 비교로 답하고 학습 비용은 다섯 칸 중 가장 크다.

헤드라인 수치는 회복률이다.

```
회복률 = (연합 − 로컬) / (중앙집중 − 로컬) × 100%
```

기준 지표는 결과를 보기 전에 Macro-F1으로 고정했다. 결과를 확인한 뒤 유리한 지표를
고르지 못하게 막기 위해서다.

## 재현을 위해 고정한 것

다섯 칸을 같은 조건에서 돌렸기 때문에 이 연구의 주장이 선다. 그래서 아래를 규칙으로 두었다.

- 조기 종료를 쓰지 않는다. 채점 대상은 마지막 체크포인트다. 학습량 등가 `R × E = N`이 공정성의 근거다.
- 평가셋은 회사별 분할보다 먼저 떼어낸다. 같은 용접부에서 나온 이미지는 묶음 단위로 나눈다.
- 다섯 칸의 출력을 공통 스키마로 바꾼 뒤 하나의 채점기로 채점한다.
- 생성 모델이 자기 결과를 검증하지 않는다. 추론은 greedy 한 번이고 재시도하지 않는다.
- corpus와 색인, 분할에 스냅샷 해시를 붙이고 재생성하지 않는다.

프레임워크 기본값이 위 선언을 조용히 무너뜨리는 경우를 여섯 건 찾아 처방과 함께 기록했다
([숨은 기본값 레지스트리](docs/dev_log/2026-08-17-kickoff/22_숨은기본값_레지스트리_C.md)).
실사용을 기록하고 불일치는 실패로 올린다.

## 저장소 구조

```
configs/      라벨 사상표, GPU 프로파일, 실험 설정
data/         수집 → 변환 → 중복 묶음 → 분할 → 매니페스트
corpus/       규정 파싱, 허용치 표, 합성 데이터 생성과 3단계 검증
detection/    분리형 검출 학습
vlm/          통합형 학습과 좌표 규약
fl/           연합학습 서버·클라이언트, 집계
rag/          조항 단위 색인과 검색
evaluation/   공통 예측 스키마, 단일 채점기, 지표
tracking/     실험 추적, 체크포인트 규칙
docs/         설계 문서와 개발 기록
```

## 데이터

원본 이미지와 가중치, 학습 산출물은 저장소에 넣지 않는다. 매니페스트와 스냅샷 해시만 추적한다.

규정 문서는 원문을 재배포하지 않는다. 파싱 스크립트와 출처, 판본, 해시를
`corpus/parse/sources.yaml`에 두어 원문을 직접 받으면 같은 결과가 나오게 했다.
저장소에는 조항 번호와 허용치 수치처럼 사실에 해당하는 정보만 둔다.

## 개발 기록

`docs/dev_log/`에 설계 명세와 교차 리뷰, 게이트 판정을 남겼다.
어떤 결정을 왜 내렸는지, 무엇이 반려됐는지가 그대로 있다. 중간에 뒤집힌 판단까지 그대로다.

## 인용 의무

사용하는 데이터셋의 인용 요건을 따른다. RIAWELC을 쓰면 Totino 외(2022)와
Perri 외(Manufacturing Letters), Intel Robotic Welding을 쓰면 Stemmer 외(2024,
arXiv:2409.02290)를 인용한다. 상세는 [`docs/논문_리스트.md`](docs/논문_리스트.md)에 있다.

## 라이선스

AGPL-3.0. 전문은 [`LICENSE`](LICENSE)에 있다.

검출에 쓰는 Ultralytics YOLO11이 AGPL-3.0이라 같은 조건을 따른다. 이 코드를 가져다
쓰면 파생물도 소스를 공개해야 한다. 사업화 단계에서는 검출기를 Apache 계열로 갈아끼우고
재라이선싱하는 것을 전제로 두었다.

의존 라이브러리는 각자의 조건을 따른다. Flower는 Apache-2.0이다.

## 팀

조현건(총괄·연합학습), 김예서(통합형·판정 생성), 박상은(데이터·검출·평가)

